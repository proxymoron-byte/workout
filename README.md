# Workout

Health, nutrition, and fitness goal tracker. Static PWA built with Vite + React, deployed to GitHub Pages.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

Push to `main`. The `Deploy to GitHub Pages` workflow builds and publishes `dist/` to the `gh-pages` branch.
